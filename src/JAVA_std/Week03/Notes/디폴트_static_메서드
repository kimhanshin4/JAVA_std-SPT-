디폴트, static 메서드
ㄴ인터페이스 설명시 디폴트,static메서드만 예외적으로 구현하는 부분이 있어야함

디폴트 메서드 : 추상 메서드의 기본적인 '구현'을 제공하는 메서드
-메서드 앞에 default 키워드를 붙여 블럭{}이 존재해야 함
-default 메서드 역시 접근 제어자가 public이며 생략 가능 => 컴파일러 자동생성

static(인터페이스에서) 메서드 : static 특성 그대로 객체 없이 호출 가능

형변환(인터페이스에서)
-인터페이스 변수 = 구현객체; => 자동으로 타입 변환
강제 타입변환
-구현객체타입 변수 = (구현객체타입) 인터페이스변수;