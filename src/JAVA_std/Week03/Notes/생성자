Constructor
-객체 생성시 호출됨, 객체를 초기화함

기본생성자 : 선언시 괄호() 안에 아무것도 넣지 않는 생성자
ㄴ모든 클래스는 반드시 생성자가 하나 이상 존재
ㄴ컴파일러에 의해 생성되는 기본 생성자는 해당 클래스의 접근 제어자(public, ...)를 따른다.

?
┌ 객체를 만들 때 인스턴스마다 다른 값을 가져야 한다면, 생성자를 통해 필드를 초기화 할 수 있다.
└ 인스턴스 마다 동일한 데이터를 가지는 필드는 초기값을 대입하는 것이 좋다.