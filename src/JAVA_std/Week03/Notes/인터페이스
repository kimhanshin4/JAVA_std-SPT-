인터페이스
-상속 관계가 없는 타 클래스들이 서로 동일한 행위, 메서드를 구현해야 할 때, 구현 클래스들의 동일한 사용 법과 행위를 보장
ㄴ인터페이스 => 스팩이 정의된 메서드들의 집합
ㄴ공용으로서의 사용 용이

-public interface 인터페이스명 {}
-모든 멤버변수는 public static final 이어야 함
-모든 메서드는 public abstract 이어야 함
-생략되는 제어자는 컴파일러가 자동으로 추가

구현'{}'=>추상 클래스와 마찬가지로 직접 인스턴스 생성 불가능
ㄴ> 클래스에서 인터페이스를 implements키워드를 사용, 받아서 추상 메서드를 오버라이딩***
-인터페이스의 추상 메서드는 구현 될 때 반드시 오버라이딩 되어야 함
-만약 인터페이스의 추상 메서드를 일부만 구현해야 한다면 해당 클래스를 추상 클래스로 변경해주면 됨

인터페이스간의 상속 가능 ?
-인터페이스간 상속 => extends 키워드 사용
-인터페이스는 다중 상속 가능 / 일반 클래스는 불가