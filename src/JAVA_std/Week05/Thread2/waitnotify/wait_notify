침범을 막은 코드(sync)를 수행하다 더 진행할 상황이 아니다
    =>wait()를 호출, 스레드가 Lock을 반납하고 기다리게 할 수 있다.
ㄴ실행 중이던 스레드는 해당 객체의 대기실(waiting pool)에서 통지를 기다림 - notify를 받을 때 까지
wait() 와 notify()는 짝꿍이다.

병목현상에 주의한다.

notify() - 해당 객체의 대기실(waiting pool)에 있는 모든 스레드 중에서 임의의 통지를 받는다.
