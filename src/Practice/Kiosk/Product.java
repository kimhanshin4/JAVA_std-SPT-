package Practice.Kiosk;

// 1. 만들려고 하는 객체의 설계도.(클래스 선언)
public class Product {
// 2. 객체가 가지고 있어야 할 속성(필드) 정의
    //<필드 영역>
    int menuId;
    String menuName;
    String price;
    String ingredients;

// 3. 객체를 생성하는 방식을 정의.(생성자) => 변수를 넣은 필드를 어떻게 정의할 것인가.
    //    -constructor
    // 처음 객체가 생성될 때(Instance화) 어떤 로직을 수행해야 하며, 어떤 값이 필수로 들어와야 하는지 정의!
    //<생성자 영역>
    public Product () {} // 기본생성자 : 처음 객체 생성시, 어떠한 logic도 수행하지 않는 빈 형태. (생략가능)

// 4. 객체가 가지고 있어야 할 행위(메서드)를 정의. => 변수를 넣은 클래스를 제어
    //<메서드 영역>
    // buy
    //
}