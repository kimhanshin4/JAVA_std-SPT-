메서드
-행위 = 객체간의 협력을 위해 사용 => 프로그램의 유기적 동작
Var(return값이 없으면 void) testMethod () {
    return ~ ;
}
**메서드에 리턴타입을 선언해 반환할 값이 있다면 반드시 return문으로 해당 리턴타입의 반환값을 지정해줘야함
**반환값이 없을 때엔 void
    ㄴ메서드는 return문을 만나면 그대로 종료.
    ㄴvoid 타입일 때 return문을 사용해 원하는 지점에서 메서드 종료가능

매게변수
-메서드 호출시 전달하려는 값을 받기위한 변수
-메서드에 두 매개변수를 전달할 시, 순서와 타입을 맞춰 매개변수 값을 대입.
-전달할 값이 없을 시 생략 가능.

가변길이(...) -> spread 문법 => for문과 함께 사용
ㄴ매개값을 ,로 구분하여 개수 상관없이 전달 가능

-메서드도 객체로 만들어야 사용가능
내부, 외부 접근 가능

